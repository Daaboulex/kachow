---
description: Enable automatic session reflection (Stop hook triggers /reflect at end of every session)
user-invocable: false
---

Enable auto-reflect by creating the flag file:

```bash
touch ~/.claude/.reflect-enabled
```

Then confirm: "Auto-reflect enabled. Sessions will automatically capture learnings on exit."
